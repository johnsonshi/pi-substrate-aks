apiVersion: v1
kind: Namespace
metadata:
  name: pi-substrate
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pisa-model-relay
  namespace: pi-substrate
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pisa-remote-actor
  namespace: pi-substrate
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pisa-model-relay
  namespace: pi-substrate
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pisa-model-relay
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pisa-model-relay
        pisa.runtime/role: relay
      annotations:
        pisa.dev/deploy-revision: __PISA_DEPLOY_REVISION__
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        agentpool: system
      serviceAccountName: pisa-model-relay
      terminationGracePeriodSeconds: 5
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: relay
          image: __PISA_HARNESS_IMAGE__
          imagePullPolicy: Always
          command:
            - node
            - --import
            - tsx
            - packages/model-relay/src/relay-cli.ts
          env:
            - name: PISA_RELAY_ACTOR_COUNT
              value: "2"
            - name: PISA_RELAY_ACTOR_0_ID
              value: remote-implementer
            - name: PISA_RELAY_ACTOR_0_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: implementer-actor-token
            - name: PISA_RELAY_ACTOR_0_JOB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: implementer-job-token
            - name: PISA_RELAY_ACTOR_0_URL
              value: http://pisa-remote-implementer.pi-substrate.svc.cluster.local:8080/v1/run
            - name: PISA_RELAY_ACTOR_1_ID
              value: remote-reviewer
            - name: PISA_RELAY_ACTOR_1_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: reviewer-actor-token
            - name: PISA_RELAY_ACTOR_1_JOB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: reviewer-job-token
            - name: PISA_RELAY_ACTOR_1_URL
              value: http://pisa-remote-reviewer.pi-substrate.svc.cluster.local:8080/v1/run
            - name: PISA_RELAY_TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: tunnel-token
            - name: PISA_RELAY_JOB_CLIENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-relay-capabilities
                  key: job-client-token
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 2
            timeoutSeconds: 1
            failureThreshold: 30
          resources:
            requests:
              cpu: 25m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1000
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pisa-model-relay
  namespace: pi-substrate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pisa-model-relay
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pisa-remote-implementer
  namespace: pi-substrate
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pisa-remote-implementer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pisa-remote-implementer
        pisa.runtime/actor-id: remote-implementer
        pisa.runtime/isolation: kata
        pisa.runtime/role: actor
      annotations:
        pisa.dev/deploy-revision: __PISA_DEPLOY_REVISION__
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    pisa.runtime/role: actor
                topologyKey: kubernetes.io/hostname
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        agentpool: sandbox
      runtimeClassName: kata-vm-isolation
      serviceAccountName: pisa-remote-actor
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: actor
          image: __PISA_HARNESS_IMAGE__
          imagePullPolicy: Always
          command:
            - node
            - --import
            - tsx
            - packages/remote-actor/src/remote-actor-cli.ts
          env:
            - name: PISA_ACTOR_ID
              value: remote-implementer
            - name: PISA_ACTOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-actor-implementer-capabilities
                  key: actor-token
            - name: PISA_JOB_TOKEN_SHA256
              valueFrom:
                secretKeyRef:
                  name: pisa-actor-implementer-capabilities
                  key: actor-job-token-sha256
            - name: PISA_BROKER_URL
              value: http://pisa-model-relay.pi-substrate.svc.cluster.local:8080
            - name: PISA_WORK_ROOT
              value: /workspace
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 3
            timeoutSeconds: 1
            failureThreshold: 100
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1000
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: workspace
              mountPath: /workspace
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
        - name: workspace
          emptyDir:
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pisa-remote-implementer
  namespace: pi-substrate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pisa-remote-implementer
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pisa-remote-reviewer
  namespace: pi-substrate
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pisa-remote-reviewer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pisa-remote-reviewer
        pisa.runtime/actor-id: remote-reviewer
        pisa.runtime/isolation: kata
        pisa.runtime/role: actor
      annotations:
        pisa.dev/deploy-revision: __PISA_DEPLOY_REVISION__
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    pisa.runtime/role: actor
                topologyKey: kubernetes.io/hostname
      automountServiceAccountToken: false
      enableServiceLinks: false
      nodeSelector:
        agentpool: sandbox
      runtimeClassName: kata-vm-isolation
      serviceAccountName: pisa-remote-actor
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: actor
          image: __PISA_HARNESS_IMAGE__
          imagePullPolicy: Always
          command:
            - node
            - --import
            - tsx
            - packages/remote-actor/src/remote-actor-cli.ts
          env:
            - name: PISA_ACTOR_ID
              value: remote-reviewer
            - name: PISA_ACTOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: pisa-actor-reviewer-capabilities
                  key: actor-token
            - name: PISA_JOB_TOKEN_SHA256
              valueFrom:
                secretKeyRef:
                  name: pisa-actor-reviewer-capabilities
                  key: actor-job-token-sha256
            - name: PISA_BROKER_URL
              value: http://pisa-model-relay.pi-substrate.svc.cluster.local:8080
            - name: PISA_WORK_ROOT
              value: /workspace
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 3
            timeoutSeconds: 1
            failureThreshold: 100
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 1000
            runAsUser: 1000
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: workspace
              mountPath: /workspace
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
        - name: workspace
          emptyDir:
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: pisa-remote-reviewer
  namespace: pi-substrate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pisa-remote-reviewer
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pisa-remote-actor-isolation
  namespace: pi-substrate
spec:
  podSelector:
    matchLabels:
      pisa.runtime/role: actor
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              pisa.runtime/role: relay
      ports:
        - port: 8080
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              pisa.runtime/role: relay
      ports:
        - port: 8080
          protocol: TCP
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: pisa-remote-actor-host-deny
  namespace: pi-substrate
spec:
  endpointSelector:
    matchLabels:
      "k8s:pisa.runtime/role": actor
  egressDeny:
    - toEntities:
        - host
        - remote-node
        - kube-apiserver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pisa-model-relay-restricted-egress
  namespace: pi-substrate
spec:
  podSelector:
    matchLabels:
      pisa.runtime/role: relay
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              pisa.runtime/role: actor
      ports:
        - port: 8080
          protocol: TCP
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
